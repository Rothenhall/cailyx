# Backend API base URL — baked in at build time (Next.js NEXT_PUBLIC_*).
# Dev: the NestJS backend runs on :3002 (see backend/.env.example PORT).
# Prod: set to the real API origin.
NEXT_PUBLIC_API_URL=http://localhost:3002
