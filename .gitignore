# dependencies
node_modules/

# build output
dist/
.next/
out/
build/

# env files (never commit)
.env
.env.local
.env.*.local

# database
*.db
*.db-journal

# logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
Thumbs.db
# Redis snapshot written to the repo root when a local redis-server is
# started here; a dev artifact, never source.
dump.rdb

# Throwaway theme screenshots (not source)
brand-theme-preview/
